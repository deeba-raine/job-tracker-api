Job Application Tracker is a full-stack project I'm building to manage and monitor my job search — tracking applications, their current status, and follow-up dates in one place. The backend is built with Node.js and Express, using MySQL for data storage, with a React frontend planned once the core API is complete. The project is currently in development, with the database schema and backend routes underway.